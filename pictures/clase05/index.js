let value = 1 ==="1" ? "iguales": "No son iguales";
console.log(value)


const PASSWORD = ProcessingInstruction.env.PASSWORD ? ProcessingInstruction.env.PASSWORD : null;

// variables de MediaElementAudioSourceNode, no deben ser expuestas, deben ser encriptadas o aseguradas
// es una variable que nosotros vamos a inyectar en el sistema, pra guardar una variable podemos guardarla como asd.